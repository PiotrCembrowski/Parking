// ─── Site / Location Configuration ──────────────────────────────────────────
// Central place for per-location contact details so they can be swapped in one
// spot rather than hardcoded across pages.

// TODO(piotr): replace with real Omaha CallRail tracking number before deploy
export const OMAHA_PHONE = "(402) 000-0000";
export const OMAHA_PHONE_TEL = "tel:+14020000000";
